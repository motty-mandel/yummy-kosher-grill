{
  "name": "yummy-kosher-grill-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "serverInstall": "cd server && npm install",
    "serverStart": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build",
    "clientStart": "cd client && npm run dev"
  }
}
